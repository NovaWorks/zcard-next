version: v2
modules:
  - path: api
  - path: internal
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX  # 内部 conf 包不带版本后缀
breaking:
  use:
    - FILE
