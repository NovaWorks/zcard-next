// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/NovaWorks/zcard-next/server/internal/data/ent/lotteryrevision"
)

// LotteryRevision is the model entity for the LotteryRevision schema.
type LotteryRevision struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 租户（0=主站；由 interceptor 自动注入，业务不手写）
	SubsiteID uint64 `json:"subsite_id,omitempty"`
	// ActivityID holds the value of the "activity_id" field.
	ActivityID uint64 `json:"activity_id,omitempty"`
	// Revision holds the value of the "revision" field.
	Revision int32 `json:"revision,omitempty"`
	// AdminID holds the value of the "admin_id" field.
	AdminID uint64 `json:"admin_id,omitempty"`
	// Snapshot holds the value of the "snapshot" field.
	Snapshot     map[string]interface{} `json:"snapshot,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LotteryRevision) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case lotteryrevision.FieldSnapshot:
			values[i] = new([]byte)
		case lotteryrevision.FieldID, lotteryrevision.FieldSubsiteID, lotteryrevision.FieldActivityID, lotteryrevision.FieldRevision, lotteryrevision.FieldAdminID:
			values[i] = new(sql.NullInt64)
		case lotteryrevision.FieldCreatedAt, lotteryrevision.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LotteryRevision fields.
func (_m *LotteryRevision) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case lotteryrevision.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint64(value.Int64)
		case lotteryrevision.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case lotteryrevision.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case lotteryrevision.FieldSubsiteID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field subsite_id", values[i])
			} else if value.Valid {
				_m.SubsiteID = uint64(value.Int64)
			}
		case lotteryrevision.FieldActivityID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field activity_id", values[i])
			} else if value.Valid {
				_m.ActivityID = uint64(value.Int64)
			}
		case lotteryrevision.FieldRevision:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field revision", values[i])
			} else if value.Valid {
				_m.Revision = int32(value.Int64)
			}
		case lotteryrevision.FieldAdminID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field admin_id", values[i])
			} else if value.Valid {
				_m.AdminID = uint64(value.Int64)
			}
		case lotteryrevision.FieldSnapshot:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field snapshot", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Snapshot); err != nil {
					return fmt.Errorf("unmarshal field snapshot: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LotteryRevision.
// This includes values selected through modifiers, order, etc.
func (_m *LotteryRevision) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this LotteryRevision.
// Note that you need to call LotteryRevision.Unwrap() before calling this method if this LotteryRevision
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *LotteryRevision) Update() *LotteryRevisionUpdateOne {
	return NewLotteryRevisionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the LotteryRevision entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *LotteryRevision) Unwrap() *LotteryRevision {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: LotteryRevision is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *LotteryRevision) String() string {
	var builder strings.Builder
	builder.WriteString("LotteryRevision(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("subsite_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SubsiteID))
	builder.WriteString(", ")
	builder.WriteString("activity_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ActivityID))
	builder.WriteString(", ")
	builder.WriteString("revision=")
	builder.WriteString(fmt.Sprintf("%v", _m.Revision))
	builder.WriteString(", ")
	builder.WriteString("admin_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AdminID))
	builder.WriteString(", ")
	builder.WriteString("snapshot=")
	builder.WriteString(fmt.Sprintf("%v", _m.Snapshot))
	builder.WriteByte(')')
	return builder.String()
}

// LotteryRevisions is a parsable slice of LotteryRevision.
type LotteryRevisions []*LotteryRevision
