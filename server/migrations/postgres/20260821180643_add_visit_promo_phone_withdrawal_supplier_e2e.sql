-- Modify "recharge_orders" table
ALTER TABLE "recharge_orders" ADD COLUMN "supplier_account_id" bigint NULL;
-- Modify "supplier_accounts" table
ALTER TABLE "supplier_accounts" ALTER COLUMN "protocol" TYPE character varying, ALTER COLUMN "display_name" TYPE character varying, ADD COLUMN "owner_user_id" bigint NULL DEFAULT 0, ADD COLUMN "apply_reason" character varying NULL, ADD COLUMN "review_note" character varying NULL, ADD COLUMN "ip_whitelist" jsonb NULL;
-- Create index "supplieraccount_owner_user_id" to table: "supplier_accounts"
CREATE INDEX "supplieraccount_owner_user_id" ON "supplier_accounts" ("owner_user_id");
-- Modify "users" table
ALTER TABLE "users" ADD COLUMN "phone" character varying NULL, ADD COLUMN "promo_code" character varying NULL;
-- Create index "users_phone_key" to table: "users"
CREATE UNIQUE INDEX "users_phone_key" ON "users" ("phone");
-- Create index "users_promo_code_key" to table: "users"
CREATE UNIQUE INDEX "users_promo_code_key" ON "users" ("promo_code");
-- Modify "withdrawals" table
ALTER TABLE "withdrawals" ADD COLUMN "receipt" character varying NULL;
-- Create "page_views" table
CREATE TABLE "page_views" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "day" character varying NOT NULL, "path" character varying NOT NULL, "user_id" bigint NULL, "ip" character varying NOT NULL, PRIMARY KEY ("id"));
-- Create index "pageview_day_ip" to table: "page_views"
CREATE INDEX "pageview_day_ip" ON "page_views" ("day", "ip");
-- Create index "pageview_day_subsite_id" to table: "page_views"
CREATE INDEX "pageview_day_subsite_id" ON "page_views" ("day", "subsite_id");
-- Create "user_sessions" table
CREATE TABLE "user_sessions" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "user_id" bigint NOT NULL, "ip" character varying NOT NULL, "last_active_at" timestamptz NOT NULL, PRIMARY KEY ("id"));
-- Create index "usersession_last_active_at" to table: "user_sessions"
CREATE INDEX "usersession_last_active_at" ON "user_sessions" ("last_active_at");
-- Create index "usersession_subsite_id_user_id" to table: "user_sessions"
CREATE UNIQUE INDEX "usersession_subsite_id_user_id" ON "user_sessions" ("subsite_id", "user_id");
