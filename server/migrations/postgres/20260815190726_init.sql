-- Create "role_permissions" table
CREATE TABLE "role_permissions" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "role_id" bigint NOT NULL, "permission_code" character varying NOT NULL, PRIMARY KEY ("id"));
-- Create index "rolepermission_role_id_permission_code" to table: "role_permissions"
CREATE UNIQUE INDEX "rolepermission_role_id_permission_code" ON "role_permissions" ("role_id", "permission_code");
-- Create "admin_users" table
CREATE TABLE "admin_users" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "username" character varying NOT NULL, "password_hash" character varying NOT NULL, "nickname" character varying NULL, "avatar" character varying NULL, "role_id" bigint NOT NULL, "totp_secret" bytea NULL, "enabled" boolean NOT NULL DEFAULT true, "remark" character varying NULL, "last_login_ip" character varying NULL, "last_login_at" timestamptz NULL, PRIMARY KEY ("id"));
-- Create index "admin_users_username_key" to table: "admin_users"
CREATE UNIQUE INDEX "admin_users_username_key" ON "admin_users" ("username");
-- Create index "adminuser_role_id" to table: "admin_users"
CREATE INDEX "adminuser_role_id" ON "admin_users" ("role_id");
-- Create "currencies" table
CREATE TABLE "currencies" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "code" character varying NOT NULL, "symbol" character varying NOT NULL, "position" character varying NOT NULL DEFAULT 'prefix', "precision" integer NOT NULL DEFAULT 2, "rate" numeric(20,8) NOT NULL DEFAULT 1, "enabled" boolean NOT NULL DEFAULT true, "sort" integer NOT NULL DEFAULT 0, PRIMARY KEY ("id"));
-- Create index "currencies_code_key" to table: "currencies"
CREATE UNIQUE INDEX "currencies_code_key" ON "currencies" ("code");
-- Create index "currency_enabled" to table: "currencies"
CREATE INDEX "currency_enabled" ON "currencies" ("enabled");
-- Create "users" table
CREATE TABLE "users" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "username" character varying NOT NULL, "email" character varying NULL, "password_hash" character varying NULL, "status" character varying NOT NULL DEFAULT 'active', "last_login_at" timestamptz NULL, "invite_l1" bigint NULL, "invite_l2" bigint NULL, "invite_l3" bigint NULL, PRIMARY KEY ("id"));
-- Create index "user_invite_l1" to table: "users"
CREATE INDEX "user_invite_l1" ON "users" ("invite_l1");
-- Create index "users_email_key" to table: "users"
CREATE UNIQUE INDEX "users_email_key" ON "users" ("email");
-- Create index "users_username_key" to table: "users"
CREATE UNIQUE INDEX "users_username_key" ON "users" ("username");
-- Create "settings" table
CREATE TABLE "settings" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "group" character varying NOT NULL, "key" character varying NOT NULL, "value" jsonb NOT NULL, PRIMARY KEY ("id"));
-- Create index "setting_group_key" to table: "settings"
CREATE UNIQUE INDEX "setting_group_key" ON "settings" ("group", "key");
-- Create "products" table
CREATE TABLE "products" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "category_id" bigint NULL, "name" character varying NOT NULL, "slug" character varying NOT NULL, "description" text NULL, "cover" character varying NULL, "images" jsonb NULL, "price" bigint NOT NULL DEFAULT 0, "factory_price" bigint NOT NULL DEFAULT 0, "draft_premium" bigint NOT NULL DEFAULT 0, "member_price" jsonb NULL, "stock_type" character varying NOT NULL DEFAULT 'card', "stock_visible" boolean NOT NULL DEFAULT true, "delivery_mode" character varying NOT NULL DEFAULT 'status', "control_config" jsonb NULL, "dedup" boolean NOT NULL DEFAULT true, "sort" integer NOT NULL DEFAULT 0, "status" smallint NOT NULL DEFAULT 1, "upstream_source_id" bigint NULL, "upstream_product_code" character varying NULL, "upstream_synced_at" timestamptz NULL, PRIMARY KEY ("id"));
-- Create index "product_subsite_id_category_id" to table: "products"
CREATE INDEX "product_subsite_id_category_id" ON "products" ("subsite_id", "category_id");
-- Create index "product_subsite_id_slug" to table: "products"
CREATE UNIQUE INDEX "product_subsite_id_slug" ON "products" ("subsite_id", "slug");
-- Create index "product_subsite_id_status" to table: "products"
CREATE INDEX "product_subsite_id_status" ON "products" ("subsite_id", "status");
-- Create index "product_upstream_source_id" to table: "products"
CREATE INDEX "product_upstream_source_id" ON "products" ("upstream_source_id");
-- Create "wallet_accounts" table
CREATE TABLE "wallet_accounts" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "user_id" bigint NOT NULL, "currency" character varying NOT NULL DEFAULT 'CNY', "available" bigint NOT NULL DEFAULT 0, "locked" bigint NOT NULL DEFAULT 0, "version" integer NOT NULL DEFAULT 0, "updated_at" timestamptz NOT NULL, PRIMARY KEY ("id"));
-- Create index "wallet_accounts_user_id_key" to table: "wallet_accounts"
CREATE UNIQUE INDEX "wallet_accounts_user_id_key" ON "wallet_accounts" ("user_id");
-- Create "wallet_transactions" table
CREATE TABLE "wallet_transactions" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "user_id" bigint NOT NULL, "direction" character varying NOT NULL, "type" character varying NOT NULL, "amount" bigint NOT NULL, "balance_before" bigint NOT NULL, "balance_after" bigint NOT NULL, "currency" character varying NOT NULL DEFAULT 'CNY', "reference" character varying NOT NULL, "order_id" bigint NULL, "operator_id" bigint NULL, "remark" character varying NULL, "created_at" timestamptz NOT NULL, PRIMARY KEY ("id"));
-- Create index "wallet_transactions_reference_key" to table: "wallet_transactions"
CREATE UNIQUE INDEX "wallet_transactions_reference_key" ON "wallet_transactions" ("reference");
-- Create index "wallettransaction_type_created_at" to table: "wallet_transactions"
CREATE INDEX "wallettransaction_type_created_at" ON "wallet_transactions" ("type", "created_at");
-- Create index "wallettransaction_user_id_created_at" to table: "wallet_transactions"
CREATE INDEX "wallettransaction_user_id_created_at" ON "wallet_transactions" ("user_id", "created_at");
-- Create "outbox_events" table
CREATE TABLE "outbox_events" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "module" character varying NOT NULL, "type" character varying NOT NULL, "aggregate_id" character varying NOT NULL, "payload" jsonb NULL, "dedupe_key" character varying NOT NULL, "status" character varying NOT NULL DEFAULT 'publishing', "published_at" timestamptz NULL, "created_at" timestamptz NOT NULL, PRIMARY KEY ("id"));
-- Create index "outbox_events_dedupe_key_key" to table: "outbox_events"
CREATE UNIQUE INDEX "outbox_events_dedupe_key_key" ON "outbox_events" ("dedupe_key");
-- Create index "outboxevent_status_created_at" to table: "outbox_events"
CREATE INDEX "outboxevent_status_created_at" ON "outbox_events" ("status", "created_at");
-- Create "admin_roles" table
CREATE TABLE "admin_roles" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "name" character varying NOT NULL, "code" character varying NOT NULL, "description" character varying NULL, "is_builtin" boolean NOT NULL DEFAULT false, PRIMARY KEY ("id"));
-- Create index "admin_roles_code_key" to table: "admin_roles"
CREATE UNIQUE INDEX "admin_roles_code_key" ON "admin_roles" ("code");
-- Create index "admin_roles_name_key" to table: "admin_roles"
CREATE UNIQUE INDEX "admin_roles_name_key" ON "admin_roles" ("name");
-- Create "payment_channels" table
CREATE TABLE "payment_channels" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "name" character varying NOT NULL, "code" character varying NOT NULL, "driver" character varying NOT NULL, "config" bytea NOT NULL, "fee" bigint NOT NULL DEFAULT 0, "fee_type" character varying NOT NULL DEFAULT 'fixed', "fee_bearer" character varying NOT NULL DEFAULT 'merchant', "sort" integer NOT NULL DEFAULT 0, "enabled" boolean NOT NULL DEFAULT true, PRIMARY KEY ("id"));
-- Create index "paymentchannel_subsite_id_code" to table: "payment_channels"
CREATE UNIQUE INDEX "paymentchannel_subsite_id_code" ON "payment_channels" ("subsite_id", "code");
-- Create "processed_events" table
CREATE TABLE "processed_events" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "event_id" bigint NOT NULL, "consumer" character varying NOT NULL, "processed_at" timestamptz NOT NULL, PRIMARY KEY ("id"));
-- Create index "processedevent_event_id_consumer" to table: "processed_events"
CREATE UNIQUE INDEX "processedevent_event_id_consumer" ON "processed_events" ("event_id", "consumer");
-- Create "cards" table
CREATE TABLE "cards" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "version" integer NOT NULL DEFAULT 0, "content" bytea NOT NULL, "content_hash" character varying NOT NULL, "number_hash" character varying NULL, "status" character varying NOT NULL DEFAULT 'available', "order_id" bigint NULL, "sku_id" bigint NULL, "import_id" bigint NULL, "card_type" character varying NULL, "note" character varying NULL, "owner_id" bigint NOT NULL DEFAULT 0, "draft_premium" bigint NOT NULL DEFAULT 0, "draft_cost" bigint NOT NULL DEFAULT 0, "price" bigint NULL, "locked_at" timestamptz NULL, "used_at" timestamptz NULL, "product_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "cards_products_cards" FOREIGN KEY ("product_id") REFERENCES "products" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "card_import_id" to table: "cards"
CREATE INDEX "card_import_id" ON "cards" ("import_id");
-- Create index "card_order_id" to table: "cards"
CREATE INDEX "card_order_id" ON "cards" ("order_id");
-- Create index "card_product_id_status" to table: "cards"
CREATE INDEX "card_product_id_status" ON "cards" ("product_id", "status");
-- Create index "card_subsite_id_product_id_content_hash" to table: "cards"
CREATE UNIQUE INDEX "card_subsite_id_product_id_content_hash" ON "cards" ("subsite_id", "product_id", "content_hash");
-- Create index "card_subsite_id_status" to table: "cards"
CREATE INDEX "card_subsite_id_status" ON "cards" ("subsite_id", "status");
-- Create "orders" table
CREATE TABLE "orders" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "version" integer NOT NULL DEFAULT 0, "order_no" character varying NOT NULL, "subsite_domain" character varying NULL, "subsite_profit" bigint NOT NULL DEFAULT 0, "profit_eligible" boolean NOT NULL DEFAULT true, "user_id" bigint NULL, "guest_contact" character varying NULL, "query_password_hash" character varying NULL, "status" character varying NOT NULL DEFAULT 'pending_payment', "total_amount" bigint NOT NULL DEFAULT 0, "cost" bigint NOT NULL DEFAULT 0, "base_currency" character varying NULL, "display_currency" character varying NULL, "exchange_rate" numeric(20,8) NULL, "amount_display" bigint NULL, "payment_channel" character varying NULL, "contact" character varying NULL, "client_ip" character varying NULL, "risk_ip" character varying NULL, "risk_flags" jsonb NULL, "parent_id" bigint NULL, "escrow_id" bigint NULL, "invite_l1" bigint NULL, "invite_l2" bigint NULL, "invite_l3" bigint NULL, "extra" jsonb NULL, "paid_at" timestamptz NULL, "closed_at" timestamptz NULL, "expired_at" timestamptz NULL, PRIMARY KEY ("id"));
-- Create index "order_escrow_id" to table: "orders"
CREATE INDEX "order_escrow_id" ON "orders" ("escrow_id");
-- Create index "order_invite_l1" to table: "orders"
CREATE INDEX "order_invite_l1" ON "orders" ("invite_l1");
-- Create index "order_parent_id" to table: "orders"
CREATE INDEX "order_parent_id" ON "orders" ("parent_id");
-- Create index "order_risk_ip_user_id_status" to table: "orders"
CREATE INDEX "order_risk_ip_user_id_status" ON "orders" ("risk_ip", "user_id", "status");
-- Create index "order_status_expired_at" to table: "orders"
CREATE INDEX "order_status_expired_at" ON "orders" ("status", "expired_at");
-- Create index "order_subsite_id_created_at" to table: "orders"
CREATE INDEX "order_subsite_id_created_at" ON "orders" ("subsite_id", "created_at");
-- Create index "order_user_id_status" to table: "orders"
CREATE INDEX "order_user_id_status" ON "orders" ("user_id", "status");
-- Create index "orders_order_no_key" to table: "orders"
CREATE UNIQUE INDEX "orders_order_no_key" ON "orders" ("order_no");
-- Create "order_amount_lines" table
CREATE TABLE "order_amount_lines" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "item_id" bigint NULL, "type" character varying NOT NULL, "amount" bigint NOT NULL, "source_type" character varying NULL, "source_id" bigint NULL, "seq" integer NOT NULL DEFAULT 0, "meta" jsonb NULL, "created_at" timestamptz NOT NULL, "order_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "order_amount_lines_orders_amount_lines" FOREIGN KEY ("order_id") REFERENCES "orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "orderamountline_item_id" to table: "order_amount_lines"
CREATE INDEX "orderamountline_item_id" ON "order_amount_lines" ("item_id");
-- Create index "orderamountline_order_id" to table: "order_amount_lines"
CREATE INDEX "orderamountline_order_id" ON "order_amount_lines" ("order_id");
-- Create "order_deliveries" table
CREATE TABLE "order_deliveries" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "item_id" bigint NOT NULL, "card_id" bigint NOT NULL, "delivery_token_hash" character varying NOT NULL, "delivered_mode" character varying NOT NULL, "delivered_by" bigint NOT NULL DEFAULT 0, "logistics" jsonb NULL, "fetch_count" integer NOT NULL DEFAULT 0, "delivered_at" timestamptz NULL, "fetched_ip" character varying NULL, "order_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "order_deliveries_orders_deliveries" FOREIGN KEY ("order_id") REFERENCES "orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "orderdelivery_card_id" to table: "order_deliveries"
CREATE INDEX "orderdelivery_card_id" ON "order_deliveries" ("card_id");
-- Create index "orderdelivery_order_id" to table: "order_deliveries"
CREATE INDEX "orderdelivery_order_id" ON "order_deliveries" ("order_id");
-- Create "order_items" table
CREATE TABLE "order_items" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "product_id" bigint NOT NULL, "sku_id" bigint NULL, "sku_name" character varying NULL, "unit_price" bigint NOT NULL, "quantity" integer NOT NULL, "amount" bigint NOT NULL, "cost" bigint NOT NULL DEFAULT 0, "fulfillment_type" character varying NOT NULL, "fulfillment_status" character varying NOT NULL DEFAULT 'pending', "commission_snapshot" jsonb NULL, "profit_snapshot" jsonb NULL, "order_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "order_items_orders_items" FOREIGN KEY ("order_id") REFERENCES "orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "orderitem_order_id" to table: "order_items"
CREATE INDEX "orderitem_order_id" ON "order_items" ("order_id");
-- Create index "orderitem_product_id" to table: "order_items"
CREATE INDEX "orderitem_product_id" ON "order_items" ("product_id");
-- Create "order_status_events" table
CREATE TABLE "order_status_events" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "from_status" character varying NOT NULL, "to_status" character varying NOT NULL, "event" character varying NOT NULL, "operator" character varying NOT NULL, "operator_id" bigint NULL, "reason" character varying NULL, "client_ip" character varying NULL, "created_at" timestamptz NOT NULL, "order_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "order_status_events_orders_status_events" FOREIGN KEY ("order_id") REFERENCES "orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "orderstatusevent_event" to table: "order_status_events"
CREATE INDEX "orderstatusevent_event" ON "order_status_events" ("event");
-- Create index "orderstatusevent_order_id_created_at" to table: "order_status_events"
CREATE INDEX "orderstatusevent_order_id_created_at" ON "order_status_events" ("order_id", "created_at");
-- Create "payments" table
CREATE TABLE "payments" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "channel" character varying NOT NULL, "channel_order_no" character varying NULL, "amount" bigint NOT NULL, "charged_amount" bigint NOT NULL DEFAULT 0, "fee" bigint NOT NULL DEFAULT 0, "status" character varying NOT NULL DEFAULT 'pending', "paid_at" timestamptz NULL, "raw" jsonb NULL, "idempotency_key" character varying NULL, "order_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "payments_orders_payments" FOREIGN KEY ("order_id") REFERENCES "orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "payment_channel_channel_order_no" to table: "payments"
CREATE UNIQUE INDEX "payment_channel_channel_order_no" ON "payments" ("channel", "channel_order_no");
-- Create index "payment_order_id" to table: "payments"
CREATE INDEX "payment_order_id" ON "payments" ("order_id");
-- Create index "payment_status_created_at" to table: "payments"
CREATE INDEX "payment_status_created_at" ON "payments" ("status", "created_at");
-- Create "product_skus" table
CREATE TABLE "product_skus" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "subsite_id" bigint NOT NULL DEFAULT 0, "name" character varying NOT NULL, "spec_values" jsonb NOT NULL, "price" bigint NULL, "cost" bigint NULL, "stock_offset" integer NOT NULL DEFAULT 0, "upstream_sku_id" character varying NULL, "product_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "product_skus_products_skus" FOREIGN KEY ("product_id") REFERENCES "products" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "productsku_product_id" to table: "product_skus"
CREATE INDEX "productsku_product_id" ON "product_skus" ("product_id");
-- Create index "productsku_product_id_name" to table: "product_skus"
CREATE UNIQUE INDEX "productsku_product_id_name" ON "product_skus" ("product_id", "name");
-- Create "refund_orders" table
CREATE TABLE "refund_orders" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, "amount" bigint NOT NULL, "channel" character varying NOT NULL, "status" character varying NOT NULL DEFAULT 'created', "reason" text NULL, "operator_id" bigint NULL, "upstream_refund_id" character varying NULL, "order_id" bigint NOT NULL, PRIMARY KEY ("id"), CONSTRAINT "refund_orders_orders_refunds" FOREIGN KEY ("order_id") REFERENCES "orders" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION);
-- Create index "refundorder_order_id" to table: "refund_orders"
CREATE INDEX "refundorder_order_id" ON "refund_orders" ("order_id");
-- Create index "refundorder_status" to table: "refund_orders"
CREATE INDEX "refundorder_status" ON "refund_orders" ("status");
